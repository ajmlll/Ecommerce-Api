# ecommerce-api

E-commerce REST API backend project built with Node.js, Express, MongoDB, and Mongoose.

## Setup & Installation

1. Copy `.env.example` to `.env` and set your configuration variables.
2. Install dependencies: `npm install`
3. Run in development mode: `npm run dev`
4. Run in production mode: `npm start`